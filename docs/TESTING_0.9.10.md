# 0.9.10 验证记录（2026-09-05）

## 变更前的设备证据

- 手机原安装版本为 0.9.9（21）。
- `enabled_accessibility_services` 为空，`accessibility_enabled=0`；系统未绑定净启无障碍服务。
- 净启没有活动的 VPN 服务记录。
- 无线调试关闭。以上只能证明当时的状态，不能据此认定是系统杀后台、用户主动关闭或某次更新造成。

## 本地验证

- `testDebugUnitTest`：34 项，0 失败、0 错误、0 跳过。
- `lintDebug`：0 错误；7 条现有依赖版本更新建议。
- `assemblePreview`：成功。
- 保留微信、京东、金融应用 VPN 兼容策略及 DNS 转发实现。
- 新增的京东、酷狗、淘宝目录项复用原有保守文字匹配，不是新增已经实测验证的专用坐标/视觉规则。

## 实机验证边界

- 已通过 USB 覆盖安装 0.9.10（22），保留原应用数据。
- 本次手机仍在锁屏、无障碍未授权阶段，尚未完成三个应用的开屏回归；不将安装成功或历史操作计数当作“广告已解决”。
- 不通过 ADB 写安全设置替用户开启无障碍，不启动无线调试来掩盖日常保护未运行。

## 用户完成无障碍授权后需执行

1. 首页启动 VPN，确认日常保护 2/2，并观察无障碍心跳。
2. 无线调试保持关闭，离开净启/设置，分别冷启动京东、酷狗、淘宝；记录实际广告出现和消失、操作计数变化及是否误触。
3. 无可重复广告样本时标记“未复现”，不能标记“已修复”。
4. 验证京东商品、微信小程序不回归；不进入支付/账户敏感操作。
5. 熄屏/解锁、返回桌面后再测。手机重启、长时间后台和覆盖升级后的恢复还需单独验证，不能用短测代替。

诊断接口只输出本应用服务状态和规则目录中的标识，不收集页面文字或截图。操作被接收/手势完成的计数不等于广告消失验证。
