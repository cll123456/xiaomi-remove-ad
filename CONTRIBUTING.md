# 参与贡献

感谢帮助净启改善规则、兼容性、安全性和文档。

## 开始前

1. 搜索现有 Issue，避免重复；
2. 广告规则请提供应用包名、版本和最小复现步骤；
3. 不要提交包含账号、余额、真实姓名、手机号、银行卡、短信或支付信息的截图；
4. 安全漏洞按照 `SECURITY.md` 私下报告。

## 构建和测试

```powershell
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
.\gradlew.bat testDebugUnitTest assembleDebug
```

提交前必须保证测试和 Debug 构建通过。

## 规则贡献

- 优先使用无障碍文字、资源 ID 和可点击父节点；
- 不为未知页面加入固定坐标盲点；
- 金融应用只允许严格规则；
- 新增视觉模板必须同时验证有广告和无广告启动；
- 规则不能包含 Shell、脚本或下载执行代码；
- 说明已测试的应用版本、设备和系统版本。

## 代码风格

- Kotlin 优先使用小而明确的类型；
- 特权 API 使用固定 ID 和强类型方法；
- 新权限必须同时更新 Manifest、界面说明、README 和 PRIVACY；
- 失败结果必须可见，不能用笼统的“成功”掩盖部分失败；
- 可逆系统修改必须先快照后执行。

## 许可证

提交代码即表示你有权提交该内容，并同意按项目对应目录的开源许可证发布。不要复制许可证不兼容的代码、图标或广告素材。
